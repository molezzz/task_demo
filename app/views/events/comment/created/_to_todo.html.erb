<h6 class="media-heading">
  <span class="glyphicon glyphicon-pushpin"></span>&nbsp;
  <%=link_to event.source.name, user_path(event.source)%>
  <span>
    回复了任务：<%=link_to event.eventable.content, todo_path(event.eventable)%>
  </span>
</h6>
<p><%=link_to event.to['content'], comment_path(event.to['id'])%></p>
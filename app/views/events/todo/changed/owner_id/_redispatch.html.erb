<h6 class="media-heading">
  <span class="glyphicon glyphicon-retweet"></span>&nbsp;
  <%=link_to event.source.name, user_path(event.source)%>
  <span>
    把任务：<%=link_to event.eventable.content, todo_path(event.eventable)%>
    重新指派给了<%=link_to event.eventable.owner.name, user_path(event.eventable.owner)%>
  </span>
</h6>
<p></p>
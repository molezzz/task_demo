<h6 class="media-heading">
  <span class="glyphicon glyphicon-hand-right"></span>&nbsp;
  <%=link_to event.source.name, user_path(event.source)%>
  <span>
    给 <%=link_to event.eventable.owner.name, user_path(event.eventable.owner)%>
    指派了任务：<%=link_to event.eventable.content, todo_path(event.eventable)%>
  </span>
</h6>
<p></p>